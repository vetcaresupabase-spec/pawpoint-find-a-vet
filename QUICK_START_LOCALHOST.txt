═══════════════════════════════════════════════════════════════════
   PAWPOINT - QUICK LOCALHOST START COMMANDS
═══════════════════════════════════════════════════════════════════

📋 COPY THIS PROMPT FOR AI ASSISTANT:

"Start localhost for PawPoint:
1. cd F:\Vetcare-Cursor\pawpoint-find-a-vet
2. Run: netstat -ano | findstr :8080
3. If output shows a PID, run: Stop-Process -Id <PID> -Force
4. Run: npm run dev (NOT in background)
5. Wait 5 seconds
6. Verify with: netstat -ano | findstr :8080
7. Confirm server is LISTENING and tell me the URL"

═══════════════════════════════════════════════════════════════════

📝 MANUAL COMMANDS (Run in PowerShell):

Step 1: Navigate to project
   cd F:\Vetcare-Cursor\pawpoint-find-a-vet

Step 2: Check if port is in use
   netstat -ano | findstr :8080

Step 3: Kill process if needed (replace PID with actual number)
   Stop-Process -Id <PID> -Force

Step 4: Start server
   npm run dev

Step 5: Access app
   http://localhost:8080

═══════════════════════════════════════════════════════════════════

🚀 ONE-LINER (Advanced):

cd F:\Vetcare-Cursor\pawpoint-find-a-vet; Get-NetTCPConnection -LocalPort 8080 -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }; Start-Sleep -Seconds 2; npm run dev

═══════════════════════════════════════════════════════════════════

✅ SERVER RUNNING CONFIRMATION:

You should see output like:
   TCP    127.0.0.1:8080         0.0.0.0:0              LISTENING

Then access: http://localhost:8080

═══════════════════════════════════════════════════════════════════

⚠️ COMMON ISSUES:

Issue: Port already in use
Fix: Stop-Process -Id <PID> -Force

Issue: Can't find PID
Fix: Get-Process node | Stop-Process -Force

Issue: Dependencies error
Fix: npm install

═══════════════════════════════════════════════════════════════════

